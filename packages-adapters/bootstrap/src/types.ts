export enum Variant {
  Primary = 'primary',
  Secondary = 'secondary',
  Success = 'success',
  Danger = 'danger',
  Warning = 'warning',
  Info = 'info',
  Light = 'light',
  Dark = 'dark',
}

export type VariantType = 'primary' | 'secondary' | 'success' | 'danger' | 'warning' | 'info' | 'light' | 'dark';

export type SizeType = 'sm' | 'lg';

export enum Size {
  ExtraSmall = 'xs',
  Small = 'sm',
  Medium = 'md',
  Large = 'lg',
  ExtraLarge = 'xl',
}

export enum Container {
  Default = 'container',
  Small = 'container-sm',
  Medium = 'container-md',
  Large = 'container-lg',
  ExtraLarge = 'container-xl',
  Fluid = 'container-fluid',
}

export enum Align {
  Left = 'left',
  Center = 'center',
  Right = 'right',
  Justify = 'justify',
}
